apiVersion: v1
kind: Service
metadata:
  name: detyrakursit-svc1
spec:
  selector:
    app: detyrakursit
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8080
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: detyrakursit-svc1
spec:
  selector:
    app: detyrakursit
  ports:
    - port: 8084
      protocol: TCP
      targetPort: 8080
  type: NodePort

# kubectl apply -f app-service.yaml 